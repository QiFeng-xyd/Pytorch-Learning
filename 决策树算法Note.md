# 决策树：
## 算法思想：
### 输入准备阶段:
给定训练数据集D,包含若干样本及其特征和标签。同时需要确定特征集合A。
### 递归构建过程:
从根节点开始,对当前节点执行以下操作。首先检查停止条件,如果当前节点的所有样本属于同一类别,则将该节点标记为叶节点并返回;如果特征集为空或所有样本在剩余特征上取值相同,则将该节点标记为叶节点,类别为样本中的多数类。如果不满足停止条件,则从特征集A中选择最优划分特征,这是算法的关键步骤。
### 节点划分（有信息增益,信息增益率,基尼指数等划分选择标准）:
根据选定的最优特征的不同取值,将数据集D划分为若干子集。对每个子集,递归调用上述过程,生成子树。
### 输出结果:
最终返回构建完成的决策树。

## 划分选择
### 信息增益
<img width="1530" height="916" alt="image" src="https://github.com/user-attachments/assets/60b9b85e-e9f3-47b6-aecb-f1576fc39626" />
